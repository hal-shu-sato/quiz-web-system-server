services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - '3000:3000'
    stdin_open: true
    tty: true # Keeps the container running for debugging
