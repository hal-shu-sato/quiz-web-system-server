services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - '4000:4000'
    stdin_open: true
    tty: true # Keeps the container running for debugging
    volumes:
      - ./files:/usr/src/app/files
